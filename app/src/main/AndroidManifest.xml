<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.billylu.android.utildemo">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!--&lt;!&ndash; Jpush &ndash;&gt;-->
    <!--<permission-->
        <!--android:name="com.maning.gankmm.permission.JPUSH_MESSAGE"-->
        <!--android:protectionLevel="signature" />-->

    <uses-permission android:name="com.maning.gankmm.permission.JPUSH_MESSAGE" />
    <uses-permission android:name="android.permission.RECEIVE_USER_PRESENT" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <!-- 壁纸 -->
    <uses-permission android:name="android.permission.SET_WALLPAPER" />

    <!-- 用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 用于访问GPS定位 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 用于获取运营商信息，用于支持提供运营商信息相关的接口 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 用于访问wifi网络信息，wifi信息会用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 用于获取wifi的获取权限，wifi信息会用来进行网络定位 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- 用于访问网络，网络定位需要上网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 用于读取手机当前的状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 用于写入缓存数据到扩展存储卡 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 用于申请调用A-GPS模块 -->
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />
    <!-- 用于申请获取蓝牙信息进行室内定位 -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <!-- Optional. Required for location feature -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- 用于开启 debug 版本的应用在6.0 系统上 层叠窗口权限 -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_TASKS" />


    <application
        android:name=".app.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/DayTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>



        <activity android:name=".MoreActivity" />
        <activity android:name=".mob.PhoneAddressActivity" />
        <activity android:name=".mob.PostCodeActivity" />
        <activity android:name=".mob.IDCardQueryActivity" />
        <activity android:name=".mob.IPQueryActivity" />
        <activity android:name=".mob.WXArticleActivity" />
        <activity android:name=".mob.BankCardActivity" />
        <activity android:name=".mob.ChineseCalendarActivity" />
        <activity android:name=".mob.OilPriceActivity" />
        <activity android:name=".mob.DictionaryActivity" />
        <activity android:name=".mob.IdiomActivity" />
        <activity android:name=".mob.HistoryTodayActivity" />
        <activity android:name=".mob.HealthActivity" />
        <activity android:name=".mob.TrainActivity" />
        <activity android:name=".mob.TrainListActivity" />
        <activity android:name=".mob.FlightActivity" />
        <activity android:name=".mob.FlightListActivity" />
        <activity android:name=".mob.WebActivity" />


    </application>

</manifest>